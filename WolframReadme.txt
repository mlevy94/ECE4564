First you need to install wolfram alpha on the RPi, use:
    sudo pip3 install wolframalpha

This should work without any red text generated on install.

To run the program:

python3 wolfram.py (Question)

